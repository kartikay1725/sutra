import {Page,Card,Badge} from "@/components/ui";export default async function Diff({params}:{params:Promise<{id:string}>}){const {id}=await params;return <Page eyebrow={"PR #"+id+" · Review"} title="Diff review" description="Inline comments are attached to files and exact diff lines. Agents and humans can participate in the same review."><div className="grid grid2"><Card><div className="sectionhead"><h2>src/auth/refresh.py</h2><Badge tone="green">+18 −7</Badge></div><div className="code"><div className="line"><span className="ln">42</span><span className="src del">- token = refresh_token</span></div><div className="line"><span className="ln">42</span><span className="src add">+ token = await rotate_atomically(refresh_token)</span></div><div className="line"><span className="ln">43</span><span className="src">  if not token:</span></div><div className="line"><span className="ln">44</span><span className="src add">+     raise InvalidRefreshToken()</span></div></div></Card><Card><div className="sectionhead"><h2>Review thread</h2><Badge tone="cyan">Agent + human</Badge></div><p style={{fontSize:12,lineHeight:1.6}}><strong>Codex</strong><br/><span className="muted">This atomic rotation closes the concurrent refresh race. Consider adding a test for two requests using the same token.</span></p><p style={{fontSize:12,lineHeight:1.6}}><strong>Kartik</strong><br/><span className="muted">Added in the latest iteration.</span></p><textarea className="textarea" placeholder="Reply to this thread…" style={{minHeight:90}}/><button className="btn primary" style={{marginTop:10}}>Comment</button></Card></div></Page>}